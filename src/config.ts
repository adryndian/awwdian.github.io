import {
  MODELS,
  DEFAULT_MODEL,
  AWS_REGION,
  isValidModelId,
  getModelConfig,
  getAllModels,
} from '@/lib/models/config';

import type { ModelDef } from '@/lib/models/config';

export {
  MODELS,
  DEFAULT_MODEL,
  AWS_REGION,
  isValidModelId,
  getModelConfig,
  getAllModels,
};

export type { ModelDef };