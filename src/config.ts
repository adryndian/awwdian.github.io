// src/config.ts - re-export untuk backward compatibility
export {
  MODELS,
  MODEL_IDS,
  DEFAULT_MODEL,
  AWS_REGION,
  isValidModelId,
  getModelConfig,
  calculateCost,
  type ModelId,
} from '@/lib/models/config';
